node_modules/
*.log
.DS_Store
snapshot.json
models-dev-snapshot.json

# Note: dist/ is INTENTIONALLY committed
# AIO downloads dist/data/models.json directly from raw.githubusercontent.com
# CI regenerates dist/ on every sync, the new files get committed
# dist/bundle/ is the npm package output - also committed
